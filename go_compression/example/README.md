# C binding for Go compression libs example

A very basic example of how to use the C binding in Zig

---

## Running (Requires Nix)

enter the Nix shell
```sh
nix develop
```

compile and run it
```sh
zig build run
```
